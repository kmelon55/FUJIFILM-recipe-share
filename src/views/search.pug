extends base 
include mixins/recipe 

block content 
    form(method="GET").search-form
        input(placeholder="Search by title", name="keyword", type="text")
        button
            i.fas.fa-search

    div.recipe-grid
        each recipe in recipes 
            +recipe(recipe)
        else 
            span.empty__message No recipes found